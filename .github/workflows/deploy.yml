name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Placeholder deploy step
        run: |
          echo "Configure deployment target and secrets."
          echo "Recommended: SSH to VPS, pull latest main, run docker compose -f docker-compose.prod.yml up -d --build."
